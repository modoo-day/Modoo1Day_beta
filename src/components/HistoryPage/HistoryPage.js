import React from 'react';
import {View, Text} from 'react-native';

const HistoryPage = () =>{
    return(
        <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
            <Text style={{fontSize: 30}}>History PAGE</Text>
        </View>
    )
}

export default HistoryPage;