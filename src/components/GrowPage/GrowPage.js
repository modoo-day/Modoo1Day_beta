import React from 'react';
import {View, Text} from 'react-native';

const GrowPage = () =>{
    return(
        <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
            <Text style={{fontSize: 30}}>Grow PAGE</Text>
        </View>
    )
}

export default GrowPage;